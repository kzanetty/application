const BASE = "/deploy";

/*
const path = {
  BASE: BASE,
  HOME: `${BASE}/`,
  CONTATO: `${BASE}/contato`,
  SERVICO: `${BASE}/servico`,
};
*/
const path = {
  HOME: "/",
  CONTATO: "/contato",
  SERVICO: "/servico",
};

export default path;
